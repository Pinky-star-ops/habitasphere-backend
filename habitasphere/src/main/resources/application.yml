server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/habitasphere_db
    username: postgres
    password: Pinky

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect